# ── Core app ──────────────────────────────────────────────────────────────────
streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
wordcloud>=1.9.0
requests>=2.31.0
openpyxl>=3.1.0

# ── NLP ───────────────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
gensim>=4.3.0
nltk>=3.8.0

# ── Transformers (CPU-only torch — keeps install under cloud limits) ──────────
transformers>=4.35.0
torch

# ── BERTopic stack — pinned to versions known to work on Streamlit Cloud ──────
sentence-transformers==2.2.2
umap-learn==0.5.3
hdbscan==0.8.33
bertopic==0.15.0
